# Credenciais do Postgres local (docker-compose). O healthcheck usa -U root -d bureau,
# entao DB_USERNAME precisa bater com o usuario das DATABASE_URL de cada servico.
DB_USERNAME=root
DB_PASSWORD=123456

PGADMIN_EMAIL=admin@local.dev
PGADMIN_PASSWORD=123456
